/** Eval workbench. Wave 0 stub — expanded in T4 (map + draw), T5 (panels),
 *  T6 (calibration), T7 (cases). */
export default function EvalPage() {
  return <div data-testid="eval-page">Eval workbench (coming online)</div>
}
